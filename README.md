# minecraft-artemis-builder
A building game that allows user to build artemis using blocks, like minecraft. User understands use of each component, hunts it down, and builds the spacecraft. If built correctly, spacecraft launches to moon. User becomes part of Artemis crew and runs experiments and simulations.
